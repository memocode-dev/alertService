spring:
  profiles:
    include: secrets

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

custom:
  sns:
    access_key: "SECRET"
    secret_key: "SECRET"
  email:
    from_email: "SECRET"
    access_key: "SECRET"
    secret_key: "SECRET"